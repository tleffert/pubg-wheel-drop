import { LocationOption } from './location-option.component';

describe('LocationOption.TsDirective', () => {
  it('should create an instance', () => {
    const directive = new LocationOption();
    expect(directive).toBeTruthy();
  });
});
